# 文档工作记录：阶段 17 后现状基线同步

## 1. 工作信息

| 字段 | 内容 | 含义 |
|---|---|---|
| 工作项 | documentation-baseline-refresh | 阶段 17 完成后的存量文档一致性维护 |
| 日期 | 2026-09-21 | 实际执行日期 |
| 状态 | 已完成 | 文档校验与完整差异 Review 已通过，未提交、未推送 |
| 代码范围 | 无 | 未修改 Java、配置、数据库迁移或公共接口 |

## 2. 更新内容

- 根 README、总计划、文档索引和当前系统基线统一到阶段 17 完成后的状态，并明确阶段 10 在线摘要评测及阶段 17 真实模型对抗验证仍未执行。
- 明确文档事实优先级：当前基线和实际代码描述现状，冻结计划解释阶段决策，旧工作记录保留当时事实而不回写。
- 将一期产品、架构和骨架材料标记为历史快照，避免把当时的固定操作者、无认证和无长期记忆误读为当前边界。
- 将当前模型表同步为 Chat `qwen3.8-flash`，并补齐 Intent、Summary、Memory、Embedding、Rerank 独立端口；同步阶段 7 已冻结的 Top 30 双路召回、`RRF(k=20)`、Top 20 重排和 `0.30` 可靠阈值。
- 更新实际 Prompt 清单、HTTP Client 清单、模型超时/重试/并发说明，以及六个 Maven 模块 README 的真实职责。
- 本机生成且被 `.gitignore` 排除的 `导学-技术支持Agent.md`、`面经-技术支持Agent.md` 同步了阶段 17、安全能力和 239/47 测试口径；它们不属于本次可提交差异。

## 3. 验证结果

| 检查 | 结果 | 说明 |
|---|---|---|
| `mvn validate` | 通过 | 七个 Reactor 项目结构与 POM 均有效 |
| Markdown 本地链接检查 | 通过 | 扫描仓库 Markdown 的相对链接，未发现不存在的目标 |
| 当前状态关键词检查 | 通过 | 未残留“阶段 17 尚未执行”“198 个测试”“模块当前不包含业务代码”等现状性旧口径 |
| 模型配置一致性 | 通过 | 当前基线与 `application.yml` 的六类默认模型一致 |
| `git diff --check` | 通过 | 无空白错误；仅存在仓库既有的 LF/CRLF 转换提示 |

本次只修改文档，没有运行 `mvn test` 或 `mvn verify -Pintegration`。最近一次阶段 17 门禁仍为 239 个离线测试和 47 个集成测试全部通过，本次没有把历史结果伪装为重新执行。

## 4. Review

- Review 范围：全部受版本管理的文档差异、模块 README、新工作记录，以及本机忽略的导学/面经现状段落；对照实际 `application.yml`、`RetrievalParameters.baseline()`、Prompt 资源、Flyway V1～V8 和 HTTP 文件清单。
- 发现并处理：根 README 错写阶段 17 未执行；六个模块 README 仍是空骨架；模型专题仍写阶段 1 Chat 模型和阶段 6 检索参数；超时章节声称使用实际未接入模块的 Resilience4j 熔断；HTTP 和 Prompt 清单缺少后续阶段文件；导学与面经仍使用 198 个测试和阶段 14 工作记录作为最新证据。
- Review 结论：通过。当前说明与实际代码/配置一致，历史计划和旧工作记录没有被改写为事后事实，未发现敏感信息、业务范围扩展或代码变更。

## 5. 后续边界

- 真实在线摘要质量与真实模型安全对抗仍保持未验证，后续只有取得明确授权并执行对应在线 Profile 后才能更新结论。
- 下一阶段如改变模型、检索参数、身份边界、数据表或安全策略，必须先更新冻结方案，再同步当前系统基线。
